# Arquivo normalizado pelo MindScan Optimizer (Final Version)
# Caminho: D:\projetos-inovexa\mindscan\backend\venv\Lib\site-packages\jmespath\compat.py
# Última atualização: 2025-12-11T09:59:22.343661

import sys
import inspect
from itertools import zip_longest


text_type = str
string_type = str


def with_str_method(cls):
    # In python3, we don't need to do anything, we return a str type.
    return cls

def with_repr_method(cls):
    return cls

def get_methods(cls):
    for name, method in inspect.getmembers(cls, predicate=inspect.isfunction):
        yield name, method
